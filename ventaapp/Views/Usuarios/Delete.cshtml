@model ventaapp.Models.Usuarios

@{
    ViewData["Title"] = "Eliminar Usuario";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0"><i class="bi bi-exclamation-triangle"></i> Confirmar Eliminación</h4>
                </div>
                <div class="card-body">
                   
                        <div class="alert alert-danger" role="alert">
                            <i class="bi bi-exclamation-triangle-fill"></i>
                            <strong>¡Advertencia!</strong> Esta acción no se puede deshacer. 
                            ¿Está seguro de que desea eliminar este Usuario?
                        </div>


                    <h5 class="mb-4">Información del Usuario:</h5>

                    <dl class="row">
                        <dt class="col-sm-4">ID Usuario:</dt>
                        <dd class="col-sm-8">@Model.IdUsuario</dd>

                        <dt class="col-sm-4">Nombre Completo:</dt>
                        <dd class="col-sm-8">
                            <strong class="text-primary">@Model.NombreCompleto</strong>
                        </dd>

                        <dt class="col-sm-4">Tipo de Documento:</dt>
                        <dd class="col-sm-8">
                            <span class="badge bg-secondary">@Model.TipoDocumento</span>
                        </dd>

                        <dt class="col-sm-4">Número de Documento:</dt>
                        <dd class="col-sm-8">@Model.NumeroDocumento</dd>

                        <dt class="col-sm-4">Correo Electrónico:</dt>
                        <dd class="col-sm-8">@Model.Email</dd>
                    </dl>

                    <hr class="my-4">

                    <div
                    <div class="d-flex justify-content-between">
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Cancelar
                        </a>
                        <form asp-action="Delete" method="post" class="d-inline">
                            <input type="hidden" asp-for="IdUsuario" />
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash me-1"></i> Eliminar
                            </button>
                        </form>
                    </div>
                </div>
            </div>

            <!-- Aviso de consideracion -->

                <div class="card mt-3 border-info">
                    <div class="card-body">
                        <h6 class="text-info"><i class="bi bi-lightbulb"></i> Alternativa:</h6>
                        <p class="mb-0">
                            Si solo desea ocultar este usuario temporalmente, considere implementar
                            un estado "Inactivo" en lugar de eliminarlo permanentemente.
                        </p>
                    </div>
                </div>

        </div>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
}
